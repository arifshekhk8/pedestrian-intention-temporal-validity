# Verified on Python 3.13.5 / macOS (Apple M4), CPU-only training.
# Core — needed for Tier 1 (train from the shipped tensors) and Tier 2 (rebuild from PIE).
torch==2.12.0
numpy==2.4.6
pandas==3.0.3
scikit-learn==1.9.0
scipy==1.17.1
tqdm==4.67.3

# Figures and reports.
matplotlib==3.11.0

# Live demo only (demo/). Not needed to reproduce any number in results/.
# ultralytics==8.4.68
# opencv-python==4.13.0
